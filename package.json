{
  "name": "federation-cost-directives",
  "version": "0.0.1",
  "description": "This is a starter project to be used as a base for solutions demos. Be sure to give your project a relevant name and description in this README.",
  "main": "dist/index.js",
  "types": "dist/types.d.ts",
  "scripts": {
    "compile": "tsc --build tsconfig.json",
    "compile:clean": "tsc --build tsconfig.json --clean",
    "watch": "tsc --build tsconfig.json --watch",
    "test": "jest --verbose",
    "test:clean": "jest --clearCache",
    "test:watch": "jest --verbose --watchAll",
    "testonly": "npm test",
    "coverage": "npm test -- --coverage"
  },
  "dependencies": {
    "@graphql-tools/utils": "^7.10.0",
    "apollo-server": "latest",
    "apollo-server-types": "^3.0.0",
    "graphql": "^15.4.0",
    "lodash": "^4.17.21",
    "node-fetch": "^2.6.1",
    "validator": "^13.6.0"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "@types/node": "^15.6.1",
    "jest": "^27.3.1",
    "ts-jest": "^27.0.7",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  }
}
